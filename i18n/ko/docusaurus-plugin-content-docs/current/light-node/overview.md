---
slug: /light-node/overview
title: 라이트 노드 개요
sidebar_label: 개요
sidebar_position: 1
---

# 라이트 노드 개요

**QoreChain 라이트 노드**는 전체 검증자나 아카이브 노드를 실행하지 않고 QoreChain 네트워크를 따르는 경량 클라이언트입니다. 모든 트랜잭션을 다시 재생하는 대신, 여러 RPC 엔드포인트에 걸쳐 블록 헤더를 상호 검증하고, 위임과 보상을 추적하며, 실시간 네트워크 텔레메트리를 스트리밍합니다 — 이 모든 것이 작고 독립적인 바이너리로 이루어집니다.

라이트 노드를 실행하면 전체 노드의 저장소, 대역폭, 운영 비용 없이 네트워크의 경제에 참여하고 그 상태를 관찰할 수 있습니다.

## 자체 버전 라인

라이트 노드는 **자체 버전 라인 — 현재 v3.1.2**로 제공되며, 이는 **체인 릴리스 버전과는 별개입니다**(체인은 별도의 `v3.x` 트랙에 있습니다). 바이너리는 **SHA-256 체크섬 매니페스트**와 함께 배포됩니다 — 다운로드 방식은 [메인넷 연결](/getting-started/connecting-to-mainnet)을 참조하세요 — 그리고 v3.1.2는 Windows와 macOS 바이너리가 실제로 keygen/sign/verify를 통과하는 첫 릴리스입니다(이전 빌드는 Rust 라이브러리 교체 이전에 만들어져 해당 플랫폼에서 조용히 실패했습니다). 현재는 **테스트넷** 릴리스 채널에서 승격되어 있으며, 메인넷 채널은 승격 전에 더 긴 안정화 기간을 두기 위해 의도적으로 보류되어 있습니다 — 메인넷 다운로드 링크가 404를 반환한다면 링크가 깨진 것이 아니라 이 때문입니다.

문서나 릴리스 노트를 읽을 때, 라이트 노드의 버전(v3.1.2)과 체인의 버전을 우연히 동일한 메이저 시리즈를 공유하는 두 개의 별개 숫자로 취급하세요.

## 라이트 노드를 실행하는 이유 {#why-run-a-light-node}

- **블록 보상의 일부를 획득하세요.** 활성화되고 등록된 라이트 노드는 아래에 설명된 **3% 라이트 노드 보상 분배**의 대상이 됩니다.
- **체인 상태를 직접 교차 검증하세요.** 노드는 기본 RPC 엔드포인트와 구성된 모든 감시(witness) 엔드포인트에서 최신 높이를 병렬로 가져오며, 이들이 블록 해시에 대해 일치할 때만 헤더를 저장합니다 — 단일 엔드포인트를 신뢰하는 수준에서 구성된 모든 엔드포인트가 동시에 손상되어야 하는 수준으로 기준을 높입니다. 이는 독립적인 소스 간의 상호 검증이며, **완전한 암호학적 합의 검증은 아닙니다**(검증자 집합이나 커밋 서명 확인은 하지 않습니다). 사용자의 노드는 자신이 실행 중인 모드를 **자체 대시보드**(기본값 `http://127.0.0.1:8420`)에서 `Assurance` 상태로 보고합니다 — 이는 사용자 노드의 RPC 선택이 사용자 환경에 국한되어 있기 때문에 QoreChain 중앙 대시보드에서는 확인할 수 없는 정보입니다. **`trusted-single-source`**(감시 엔드포인트 미구성)는 대부분의 운영자가 시작하는 기본값이며 경고 신호가 아닙니다 — 정직하게 운영되는 단일 엔드포인트도 손상된 엔드포인트와 동일한 값을 보고합니다. 독립적으로 운영되는 감시 엔드포인트를 추가하면 `corroborated-across-sources`로 전환됩니다.
- **위임 및 자동 복리.** 여러 검증자에 걸쳐 위임된 스테이크를 관리하고, 가중치별로 분할하며, 보상을 자동으로 복리화합니다.
- **네트워크를 실시간으로 관찰하세요.** 실시간 텔레메트리는 검증자, 합의, 브리지, 토크노믹스를 다룹니다.
- **첫날부터 포스트 양자.** 키와 서명은 Dilithium-5 (ML-DSA-87)을 사용합니다.

## 두 가지 에디션: SX와 UX

라이트 노드는 동일한 코드베이스에서 빌드된 두 가지 에디션으로 제공됩니다. 노드를 운영하려는 방식에 맞는 것을 선택하세요.

| 에디션 | 바이너리 | 대상 | 인터페이스 |
| --- | --- | --- | --- |
| **SX — Server eXperience** | `lightnode-sx` | 헤드리스 서버 배포 | 전체 CLI (데몬 + 관리 명령어) |
| **UX — User eXperience** | `lightnode-ux` | 데스크톱 및 운영자 사용 | 임베디드 웹 대시보드 |

- **SX 에디션**은 완전한 관리 CLI를 갖춘 헤드리스 데몬입니다. 서버, 자동화, 명령줄에서 활동하는 운영자에게 적합한 선택입니다. [SX 에디션](/light-node/sx-edition)을 참조하세요.
- **UX 에디션**은 동일한 데몬을 실행하지만, 브라우저에서 텔레메트리, 위임, 보상을 관찰할 수 있도록 임베디드 웹 대시보드를 추가합니다. [UX 에디션](/light-node/ux-edition)을 참조하세요.

두 에디션 모두 동일한 `config.toml`을 읽고, 동일한 홈 디렉터리(기본값 `~/.qorechain-lightnode`)에 데이터를 저장하며, 동일한 Dilithium-5 키링을 사용합니다.

## 3% 라이트 노드 보상 분배

QoreChain의 수수료 분배는 네트워크 데이터를 제공하는 라이트 노드에 고정된 **3% 분배**를 할당합니다. 이는 프로토콜의 보상 분할의 일부로 온체인에서 시행되며, 프로젝트의 경제 문서에 기록된 것과 동일한 채널입니다 — 전체 37% / 30% / 20% / 10% / 3% 분류(검증자, 소각, 트레저리, 스테이커, 라이트 노드)는 [토크노믹스](/architecture/tokenomics)를 참조하세요.

이 분배의 대상이 되려면 라이트 노드는 하트비트 증명을 통해 **온체인에 등록되고 적극적으로 활성을 증명**해야 합니다. 등록 및 라이선싱은 [등록 및 라이선싱](/light-node/registration-and-licensing)에서 다루며, 분배가 어떻게 획득되고, 복리화되며, 모니터링되는지는 [보상 및 모니터링](/light-node/rewards-and-monitoring)에서 다룹니다.

## 핵심 기능 한눈에 보기

- **다중 소스 헤더 상호 검증** — 전체 블록을 다운로드하지 않고 구성된 모든 감시 엔드포인트에 대해 최신 블록 해시를 신뢰하기 전에 교차 검증하여, 콜드 스타트에서도 빠르게 동기화합니다.
- **위임 스테이킹** — 구성 가능한 분할 가중치로 여러 검증자에 걸쳐 스테이킹합니다.
- **자동 복리 보상** — 구성 가능한 간격으로 보상을 청구하고 재위임합니다.
- **평판 인식 리밸런싱** — 더 높은 평판의 검증자로 위임을 자동으로 이동합니다.
- **실시간 텔레메트리** — 검증자, 합의, 브리지, 토크노믹스를 독립적인 간격으로 새로 고칩니다.
- **온체인 등록** — 노드를 보상 대상으로 유지하는 하트비트 활성 증명과 함께.
- **포스트 양자 암호화** — 전반에 걸친 Dilithium-5 (ML-DSA-87) 키와 서명.
- **로컬 전용 모드** — 가동 중인 체인을 가리키기 전에 전체 PQC 스택을 실행하고 노드를 독립 실행합니다.

라이트 노드는 **Apache 2.0** 라이선스로 배포됩니다.

## 다음으로 갈 곳

- [SX 에디션](/light-node/sx-edition) — 서버 데몬을 설치, 구성, 실행합니다.
- [UX 에디션](/light-node/ux-edition) — 웹 대시보드 에디션을 실행합니다.
- [등록 및 라이선싱](/light-node/registration-and-licensing) — 온체인에 등록하고 라이선스를 획득합니다.
- [보상 및 모니터링](/light-node/rewards-and-monitoring) — 3% 분배를 획득하고 노드를 건강하게 유지합니다.
- [SX 에디션](/light-node/sx-edition)과 [UX 에디션](/light-node/ux-edition)은 라이트 노드를 실행하는 두 가지 방법입니다.
- [토크노믹스](/architecture/tokenomics) — 라이트 노드 보상 분배가 더 넓은 경제에 어떻게 들어맞는지.
